# C# 프로젝트 경로 찾기

* 프로젝트 이름은 대소문자를 구분하지 않는다.
* 이미 알고 있다면 알고 있는 경로를 사용한다.
* 모른다면 임의로 검색하지 말고 *.sln 또는 *.slnx 파일을 읽어 프로젝트 경로를 찾는다.
* *.sln 또는 *.slnx에서 찾을 수 없다면 현재 경로에서 먼저 검색하고, 없다면 상위 경로로 한 단계씩 움직이며 검색한다.